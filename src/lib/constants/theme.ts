export const spacing = {
  screenPadding: 20,
  touchTarget: 48,
} as const;

export const copy = {
  appName: 'AI Tour Guide',
  noSecretsNotice: 'AI answers are requested through a secure backend. API keys are never stored in this app.',
} as const;
